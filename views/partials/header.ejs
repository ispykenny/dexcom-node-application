<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Dex-Hub - By kenny Krosky</title>
  <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet"> 
  <style>
    body {
      margin: 0;
      font-size: 17px;
      font-family: 'Roboto', sans-serif;
    }

    .initial {
      padding-top: 5em;
    }
    
    .cards-parent-outer-most, .container {
      max-width: 1400px;
      width: 90%;
      margin: auto;
    }
    .cards-parent {
      display: flex;
      padding: 2em 0 4em 0em;
      justify-content: space-around;
    }
    .cards {
      text-align: center;
      padding: 3em 2em;
      flex-basis: 30%;
      border: 1px solid #ddd;
    }
    .reading {
      font-size: 4em;
      margin: 0;
    }
    .cards p {
      text-transform: uppercase;
    }
    .cards h4 {
      margin-bottom: 0em;
    }
    .date-selectors {
      display: flex;
      list-style-type: none;
      margin: 0; padding: 0;
      padding-top: 1em;
    }
    .date-selectors li {
      margin-right: 15px;
    }

    .date-selectors a {
      /* color: #36a2eb; */
      color: #444;
      font-size: 18px;
      text-decoration: none;
    }
    .date-selectors a.active {
      color: #36a2eb;
      text-decoration: underline;
    }
    .loader-bg {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100vh;
      z-index: 5;
      text-align: center;
      background: white;
      transition: all 500ms ease;
    }  
    .loader-bg.removed {
      display: none;
    }

    .loader-bg p {
      margin: 0;
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      width: 100%; 
    }
    .twitter {
      color: #36a2eb
    }

    .logout {
      background: #36a2eb;
      color: white;
      padding: 0.6em 1.6em;
      border-radius: 8px;
      text-decoration: none;
      display: inline-block;
    }

    .content {
      max-width: 800px;
    }
  </style>
</head>
<body>